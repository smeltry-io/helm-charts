---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: addonprofiles.portal.smeltry.io
spec:
  group: portal.smeltry.io
  names:
    kind: AddonProfile
    listKind: AddonProfileList
    plural: addonprofiles
    shortNames:
    - ap
    singular: addonprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.description
      name: Description
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AddonProfile is the Schema for the addonprofiles API.
          Defined by admins in portal-system; readable by tenants.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AddonProfileSpec defines the desired state of AddonProfile.
            properties:
              components:
                description: Components is the ordered list of addons to deploy on
                  matching clusters.
                items:
                  description: AddonComponent describes one addon in a profile.
                  properties:
                    helmRef:
                      description: HelmRef points to the Helm chart to deploy for
                        this component.
                      properties:
                        chartName:
                          description: ChartName is the name of the chart within the
                            repository.
                          type: string
                        chartVersion:
                          description: ChartVersion is the exact chart version to
                            deploy (e.g. "1.16.2").
                          type: string
                        repoURL:
                          description: RepoURL is the Helm repository URL.
                          type: string
                        values:
                          description: |-
                            Values is an optional inline YAML string of Helm values.
                            For rook-ceph, OSD device mappings are merged in at reconciliation time.
                          type: string
                      required:
                      - chartName
                      - chartVersion
                      - repoURL
                      type: object
                    name:
                      description: Name is a human-readable identifier for this component
                        (e.g. "cilium", "rook-ceph").
                      type: string
                    order:
                      description: Order controls installation sequence. Lower values
                        install first.
                      minimum: 1
                      type: integer
                    required:
                      default: true
                      description: Required components must be healthy before ClusterClaim
                        reaches Ready.
                      type: boolean
                  required:
                  - helmRef
                  - name
                  - order
                  - required
                  type: object
                minItems: 1
                type: array
              description:
                description: Description is shown to tenants in the Web UI and CLI.
                type: string
              machineConstraints:
                description: MachineConstraints optionally restricts which machineClasses
                  are compatible.
                properties:
                  requiredTags:
                    description: RequiredTags are Netbox device tags that must ALL
                      be present on the machine.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - components
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
